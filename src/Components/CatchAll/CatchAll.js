const CatchAll = () => {
    return (
        <>
            <div>You have reached a page that does not exist :/</div>
            <p>click <a href="/">here </a>to go to our homepage</p>
        </>
    );
}

export default CatchAll;
